# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## Backend

This frontend expects a backend API for Project 14. Add a link to your publicly accessible backend repository here so reviewers can find it:

- Backend repository: https://github.com/<your-username>/<your-backend-repo>  <-- REPLACE with your real backend repo URL

Once you have a public backend repo, replace the placeholder above with the real GitHub URL.

This is my website --> https://closet.wtwr.verymad.net/
